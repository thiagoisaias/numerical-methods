/* Implementar e testar o método de integração da Exponencial Dupla para integração de funções
que apresentam singularidade em algum (ou em ambos) dos limites de integração.
O usuário deve fornecer como entrada:
1) os limites de integração (a e b);
2) uma tolerância (potência negativa de 10).
Obs: A função a ser integrada deve ser incluída diretamente no programa. A tolerância fornecida pelo usuário
será usada para garantir que a integral calculada entre os dois pontos de corte é precisa.
Os pontos de corte devem ser determinados em função do maior número de ponto flutuante representável
no computador do usuário. */
